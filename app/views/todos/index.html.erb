

 <main class="TodoMain">

        <article>
            <h1 class="TodoTitle">My Todo-List</h1>
            <section class="AddTodo">
             <%= form_with(url:"/todos", method: "post") do %>
               <%=text_field_tag( name='todo_text',value=nil,class:"AddTodo-Text",placeholder: "what's next?")  %>
               <%=date_field_tag(name='due_date',value=nil,class:"AddTodo-Date")  %>
               <%=button_tag(value='Add',class: "AddTodo-Button")  %>
             <%end%>
            </section>

            <%= render partial: "todo-section", locals:{
                
            section_title: "Overdue",
            todos: @todos.overdue,
            show_due_date: false,
            hide_completed: true
        
            } %>

           <%= render partial: "todo-section", locals:{
            
            section_title: "Due Today",
            todos: @todos.due_today,
            show_due_date: true,
            hide_completed: false

            
            } %>

            <%= render partial: "todo-section", locals:{
                
            section_title: "Due Later",
            todos: @todos.due_later,
            show_due_date: true,
            hide_completed: false
            
            } %>
        </article>

    </main>
